services:
  gpu-monitor:
    build: .
    ports:
      - "8001:8001"
    pid: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
